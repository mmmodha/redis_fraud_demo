[pytest]
testpaths = tests
asyncio_mode = auto
filterwarnings =
    ignore::DeprecationWarning:pydantic.*
markers =
    live: tests that hit a real network service (Anthropic, Redis Cloud). Skipped automatically when the required keys are unset.
