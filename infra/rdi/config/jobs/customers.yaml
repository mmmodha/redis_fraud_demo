name: customers
source:
  schema: public
  table: customers
output:
  - uses: redis.write
    with:
      connection: target
      data_type: json
      key: "customer:{customer_id}"
      on_update: replace
